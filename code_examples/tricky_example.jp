fun(x) {
    x = 10;
    return 10;
}

fun1(y) {
    y = 2;
    return 2;
}

main() {
  b;
  c;
  e;

  
  a = fun(@b) + fun1(@c) * b + 1;
  print(a);
}

# In this example program will go like that
#
# 1. Initialization of b with null
# 2. Initialization of c with null
# 3. Initialization of e with null
# 4. fun(@b) call
# 5. b value change to 10
# 6. fun returns 10
# 7. fun1(@c) call
# 8. c value change to 2
# 9. fun1 returns 2
# 10. Evaluate 2 * 10
# 11. Evaluate 10 + 20 + 1
# 12. Print 31