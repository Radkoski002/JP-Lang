main() {
    x = Array(1, 2, 3, 4, 5);

    sum = 0;

    print(sum)
    for(i: x) {
        sum += i;
    }
    print(sum)

    while(true) {
        sum -= 1;
        if(sum == 0) {
            break;
        }
    }
    print(sum)
}
