addTwoVariables(first, second) {
    return first + second;
}

addTwoVariablesWithOptionalParameters(first, second?) {
    if(second == null) {
        second = 0;
    }
    return first + second;
}

addTwoVariablesWithDefaultParameters(first, second? = 0) {
    return first + second;
}

main() {
    print(addTwoVariables(1, 2));
    print(addTwoVariablesWithOptionalParameters(1));
    print(addTwoVariablesWithOptionalParameters(1, 2));
    print(addTwoVariablesWithDefaultParameters(1));
    print(addTwoVariablesWithDefaultParameters(1, 2));
}